<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="#5D5CDE">
    <meta name="description" content="Advanced Model Aggregator - Compare AI model responses">
    <title>Advanced Model Aggregator</title>
    
    <!-- PWA Manifest -->
    <link rel="manifest" href="data:application/json;base64,eyJuYW1lIjoiQWR2YW5jZWQgTW9kZWwgQWdncmVnYXRvciIsInNob3J0X25hbWUiOiJBZ2dyZWdhdG9yIiwidGhlbWVfY29sb3IiOiIjNUQ1Q0RFIiwiYmFja2dyb3VuZF9jb2xvciI6IiNmZmZmZmYiLCJkaXNwbGF5Ijoic3RhbmRhbG9uZSIsInN0YXJ0X3VybCI6Ii8ifQ==">
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Roboto+Mono:wght@400;500&family=Lora:wght@400;500&display=swap" rel="stylesheet">
    
    <style>
        /* ============================================
           OPTIMIZED CSS ARCHITECTURE
           ============================================ */
        
        /* CSS Custom Properties with Fallbacks */
        :root {
            --primary: #5D5CDE;
            --primary-hover: #4A49CC;
            --bg-primary: #ffffff;
            --bg-secondary: #f7f7f7;
            --text-primary: #1a1a1a;
            --text-secondary: #666666;
            --border: #e0e0e0;
            --selected: #5D5CDE;
            --selected-bg: rgba(93, 92, 222, 0.1);
            --accent: #5D5CDE;
            --font-main: 'Inter', system-ui, -apple-system, sans-serif;
            --transition-speed: 0.2s;
            --border-radius: 8px;
            --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
            --shadow-md: 0 4px 6px rgba(0, 0, 0, 0.1);
            --shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.1);
        }

        /* Theme Definitions */
        [data-theme="dark"] {
            --bg-primary: #0f172a;
            --bg-secondary: #1e293b;
            --text-primary: #e2e8f0;
            --text-secondary: #94a3b8;
            --border: #334155;
            --accent: #8180ff;
            --selected-bg: rgba(129, 128, 255, 0.16);
        }

        [data-theme="terminal"] {
            --bg-primary: #000000;
            --bg-secondary: #0a0a0a;
            --text-primary: #34d399;
            --text-secondary: #6ee7b7;
            --border: #065f46;
            --accent: #a7f3d0;
            --selected-bg: rgba(52, 211, 153, 0.12);
            --font-main: 'Roboto Mono', monospace;
        }

        [data-theme="paper"] {
            --bg-primary: #fdf6e3;
            --bg-secondary: #f9f2df;
            --text-primary: #586e75;
            --text-secondary: #839496;
            --border: #eee8d5;
            --accent: #268bd2;
            --selected-bg: rgba(38, 139, 210, 0.12);
            --font-main: 'Lora', serif;
        }

        [data-theme="sakura"] {
            --bg-primary: #FFF5F7;
            --bg-secondary: #ffffff;
            --text-primary: #5c223a;
            --text-secondary: #9d5371;
            --border: #FECDD3;
            --accent: #EC4899;
            --selected-bg: rgba(236, 72, 153, 0.12);
        }

        [data-theme="ayu"] {
            --bg-primary: #0A0E14;
            --bg-secondary: #0F131A;
            --text-primary: #CBCCC6;
            --text-secondary: #5C6773;
            --border: #3d444f;
            --accent: #FF9940;
            --selected-bg: rgba(255, 153, 64, 0.14);
            --font-main: 'Roboto Mono', monospace;
        }

        /* Performance Optimizations */
        * {
            box-sizing: border-box;
        }

        body {
            background-color: var(--bg-primary);
            color: var(--text-primary);
            font-family: var(--font-main);
            transition: background-color var(--transition-speed), color var(--transition-speed);
            line-height: 1.5;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }

        /* Reduced Motion Support */
        @media (prefers-reduced-motion: reduce) {
            *, *::before, *::after {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
            }
        }

        /* Virtual Scrolling Container */
        .virtual-scroll-container {
            position: relative;
            overflow-y: auto;
            will-change: scroll-position;
        }

        .virtual-scroll-viewport {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            will-change: transform;
        }

        /* Accordion with GPU Acceleration */
        .accordion-container {
            position: relative;
            background: var(--bg-primary);
            border: 1px solid var(--border);
            border-radius: var(--border-radius);
            overflow: hidden;
            transition: all var(--transition-speed) ease;
            will-change: max-height;
            contain: layout style;
        }

        .accordion-container.collapsed {
            max-height: 40px;
        }

        .accordion-wrapper {
            position: relative;
            max-height: 300px;
            overflow-y: auto;
            overscroll-behavior: contain;
            opacity: 1;
            transition: opacity var(--transition-speed), max-height var(--transition-speed);
            -webkit-overflow-scrolling: touch;
            /* Prevent scroll conflict with page */
            touch-action: pan-y;
            scrollbar-width: thin;
            scrollbar-color: var(--border) transparent;
        }

        .accordion-wrapper::-webkit-scrollbar {
            width: 8px;
        }

        .accordion-wrapper::-webkit-scrollbar-track {
            background: transparent;
        }

        .accordion-wrapper::-webkit-scrollbar-thumb {
            background: var(--border);
            border-radius: 4px;
        }

        .accordion-container.collapsed .accordion-wrapper {
            max-height: 0;
            opacity: 0;
            overflow: hidden;
            pointer-events: none;
        }

        /* Selected Display with Focus States */
        .selected-display {
            padding: 12px;
            background: var(--bg-secondary);
            border-bottom: 1px solid var(--border);
            font-size: 12px;
            color: var(--text-secondary);
            min-height: 40px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            cursor: pointer;
            transition: background var(--transition-speed);
            -webkit-tap-highlight-color: transparent;
        }

        .selected-display:hover {
            background: var(--selected-bg);
        }

        .selected-display:focus-within {
            outline: 2px solid var(--accent);
            outline-offset: -2px;
        }

        .selected-display input {
            background: transparent;
            border: none;
            color: var(--text-primary);
            outline: none;
            flex: 1;
            caret-color: var(--text-primary);
            font-size: inherit;
            font-family: inherit;
        }

        /* Optimized Model Items */
        .model-item {
            padding: 6px 12px 6px 24px;
            cursor: pointer;
            transition: all var(--transition-speed);
            font-size: 11px;
            color: var(--text-primary);
            display: flex;
            align-items: center;
            gap: 6px;
            user-select: none;
            -webkit-tap-highlight-color: transparent;
            contain: layout style;
        }

        .model-item:hover {
            background: var(--selected-bg);
            padding-left: 28px;
        }

        .model-item.selected {
            background: var(--selected-bg);
            color: var(--accent);
            font-weight: 600;
        }

        .model-item.selected::before {
            content: "✓";
            position: absolute;
            left: 12px;
            color: var(--accent);
            font-weight: bold;
        }

        .model-item.highlighted {
            background: var(--selected-bg);
            box-shadow: inset 0 0 0 1px var(--accent);
        }

        /* Loading Skeleton */
        .skeleton {
            background: linear-gradient(90deg, var(--bg-secondary) 25%, var(--border) 50%, var(--bg-secondary) 75%);
            background-size: 200% 100%;
            animation: loading 1.5s infinite;
        }

        @keyframes loading {
            0% { background-position: 200% 0; }
            100% { background-position: -200% 0; }
        }

        /* Mobile-Optimized Buttons */
        .btn {
            padding: 8px 16px;
            border-radius: 6px;
            font-size: 14px;
            font-weight: 500;
            transition: all var(--transition-speed);
            cursor: pointer;
            border: 1px solid transparent;
            display: inline-flex;
            align-items: center;
            gap: 6px;
            touch-action: manipulation;
            -webkit-tap-highlight-color: transparent;
            min-height: 44px; /* iOS touch target */
        }

        .btn:active {
            transform: scale(0.98);
        }

        /* Improved Status Indicators */
        .status-indicator {
            width: 24px;
            height: 24px;
            border-radius: 50%;
            border: 2px solid var(--border);
            background: var(--bg-primary);
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all var(--transition-speed) ease;
            contain: layout style;
        }

        @keyframes spin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        .status-spinner {
            width: 12px;
            height: 12px;
            border: 2px solid var(--border);
            border-top-color: var(--accent);
            border-radius: 50%;
            animation: spin 0.8s linear infinite;
        }

        /* Responsive Grid - Force 2x2 Layout */
        .response-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            grid-template-rows: repeat(2, 1fr);
            gap: 1rem;
            contain: layout;
        }

        @media (max-width: 768px) {
            .response-grid {
                grid-template-columns: 1fr;
                grid-template-rows: repeat(4, 1fr);
            }
        }

        /* Mobile-First Response Box */
        .response-box {
            background: var(--bg-secondary);
            border-radius: var(--border-radius);
            padding: 16px;
            min-height: 320px;
            display: flex;
            flex-direction: column;
            box-shadow: var(--shadow-sm);
            contain: layout style;
        }

        @media (max-width: 640px) {
            .response-box {
                min-height: 280px;
                padding: 12px;
            }
            
            .btn-sm {
                padding: 6px;
                font-size: 11px;
            }
            
            .btn-icon {
                width: 32px;
                height: 32px;
                padding: 0;
            }
        }

        /* Smooth Scrollbar */
        .smooth-scroll {
            scroll-behavior: smooth;
            scrollbar-width: thin;
            scrollbar-color: var(--border) var(--bg-secondary);
        }

        .smooth-scroll::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        .smooth-scroll::-webkit-scrollbar-track {
            background: var(--bg-secondary);
            border-radius: 3px;
        }

        .smooth-scroll::-webkit-scrollbar-thumb {
            background: var(--border);
            border-radius: 3px;
        }

        .smooth-scroll::-webkit-scrollbar-thumb:hover {
            background: var(--text-secondary);
        }

        /* Optimized Animations */
        @keyframes slideIn {
            from {
                transform: translateX(100%);
                opacity: 0;
            }
            to {
                transform: translateX(0);
                opacity: 1;
            }
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .fade-in {
            animation: fadeIn var(--transition-speed) ease;
        }

        /* Toast with Better Positioning */
        .toast {
            position: fixed;
            top: 20px;
            right: 20px;
            background: var(--accent);
            color: white;
            padding: 12px 20px;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow-lg);
            z-index: 1000;
            animation: slideIn 0.3s ease;
            max-width: 90vw;
            word-wrap: break-word;
        }

        /* Focus Visible for Accessibility */
        :focus-visible {
            outline: 2px solid var(--accent);
            outline-offset: 2px;
        }

        /* Print Styles */
        @media print {
            .no-print {
                display: none !important;
            }
            
            .response-box {
                break-inside: avoid;
                page-break-inside: avoid;
            }
        }

        /* Responsive Container */
        .container {
            width: 100%;
            max-width: 1400px;
            margin: 0 auto;
            padding: 1rem;
        }

        @media (min-width: 640px) {
            .container { padding: 1.5rem; }
        }

        @media (min-width: 1024px) {
            .container { padding: 2rem; }
        }

        /* Performance: Contain Layout Shifts */
        img, video, iframe {
            max-width: 100%;
            height: auto;
            aspect-ratio: attr(width) / attr(height);
        }

        /* Lazy Loading Placeholder */
        .lazy-loading {
            background: var(--bg-secondary);
            min-height: 200px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--text-secondary);
        }

        /* Missing CSS for functionality */
        .btn-primary {
            background: var(--primary);
            color: white;
            border-color: var(--primary);
        }

        .btn-primary:hover {
            background: var(--primary-hover);
            border-color: var(--primary-hover);
        }

        .btn-secondary {
            background: var(--bg-secondary);
            color: var(--text-primary);
            border-color: var(--border);
        }

        .btn-secondary:hover {
            background: var(--selected-bg);
        }

        .modal-backdrop {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.5);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 1000;
        }

        .modal-content {
            background: var(--bg-primary);
            border-radius: var(--border-radius);
            padding: 1.5rem;
            max-width: 90vw;
            max-height: 90vh;
            overflow-y: auto;
            box-shadow: var(--shadow-lg);
        }

        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1rem;
        }

        .modal-title {
            font-weight: 600;
            font-size: 1.125rem;
        }

        .modal-close {
            background: none;
            border: none;
            font-size: 1.25rem;
            cursor: pointer;
            color: var(--text-secondary);
            padding: 0.25rem;
        }

        .accordion-section {
            border-bottom: 1px solid var(--border);
        }

        .accordion-header {
            padding: 8px 12px;
            cursor: pointer;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-weight: 500;
            font-size: 12px;
            color: var(--text-primary);
            background: var(--bg-secondary);
        }

        .accordion-header:hover {
            background: var(--selected-bg);
        }

        .accordion-header.active {
            background: var(--selected-bg);
            color: var(--accent);
        }

        .accordion-content {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.3s ease;
        }

        .accordion-content.active {
            max-height: 500px;
        }

        .sub-section {
            padding-left: 12px;
        }

        .sub-header {
            padding: 4px 0;
            font-size: 11px;
            font-weight: 500;
            color: var(--text-secondary);
            border-bottom: 1px solid var(--border);
            margin-bottom: 2px;
        }

        .response-content {
            background: var(--bg-primary);
            border: 1px solid var(--border);
            border-radius: 4px;
            padding: 12px;
            min-height: 200px;
            font-family: inherit;
            font-size: 14px;
            line-height: 1.5;
            white-space: pre-wrap;
            overflow-y: auto;
            max-height: 400px;
        }

        .file-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 8px 12px;
            background: var(--bg-secondary);
            border-radius: 4px;
            margin-bottom: 4px;
        }

        .file-info {
            display: flex;
            align-items: center;
            gap: 8px;
            flex: 1;
        }

        .file-name {
            font-weight: 500;
            color: var(--text-primary);
        }

        .file-size {
            font-size: 12px;
            color: var(--text-secondary);
        }

        .file-remove {
            cursor: pointer;
            color: var(--text-secondary);
            padding: 4px;
            border-radius: 4px;
        }

        .file-remove:hover {
            background: var(--border);
            color: var(--text-primary);
        }

        .log-entry {
            padding: 8px 12px;
            border-bottom: 1px solid var(--border);
            margin-bottom: 4px;
        }

        .log-timestamp {
            font-size: 11px;
            color: var(--text-secondary);
        }

        .log-title {
            font-weight: 500;
            font-size: 13px;
            color: var(--text-primary);
            margin: 2px 0;
        }

        .log-content {
            font-size: 12px;
            color: var(--text-secondary);
            font-family: 'Roboto Mono', monospace;
        }

        .log-entry.error .log-title {
            color: #ef4444;
        }

        .log-entry.user .log-title {
            color: var(--accent);
        }

        .flag-control {
            padding: 12px;
            background: var(--bg-secondary);
            border-radius: 4px;
            margin-bottom: 8px;
        }

        .flag-label {
            display: block;
            font-size: 12px;
            font-weight: 500;
            color: var(--text-primary);
            margin-bottom: 4px;
            text-transform: capitalize;
        }

        .flag-slider {
            width: 100%;
            height: 4px;
            border-radius: 2px;
            background: var(--border);
            outline: none;
            -webkit-appearance: none;
        }

        .flag-slider::-webkit-slider-thumb {
            -webkit-appearance: none;
            appearance: none;
            width: 16px;
            height: 16px;
            border-radius: 50%;
            background: var(--accent);
            cursor: pointer;
        }

        .flag-slider::-moz-range-thumb {
            width: 16px;
            height: 16px;
            border-radius: 50%;
            background: var(--accent);
            cursor: pointer;
            border: none;
        }

        .flag-value {
            font-size: 11px;
            color: var(--text-secondary);
            font-weight: 500;
            min-width: 50px;
            text-align: right;
        }

        .collapsible-section.collapsed .section-body {
            display: none;
        }

        .collapsible-section .section-arrow {
            transition: transform 0.2s ease;
        }

        .collapsible-section.collapsed .section-arrow {
            transform: rotate(-90deg);
        }

        .status-indicator.loading {
            border-color: var(--accent);
        }

        .status-indicator.success {
            border-color: #10b981;
            background: #10b981;
            color: white;
        }

        .status-indicator.error {
            border-color: #ef4444;
            background: #ef4444;
            color: white;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header with Mobile Menu -->
        <header class="flex flex-wrap justify-between items-center mb-6 gap-4">
            <div class="flex items-center gap-3">
                <button id="themeToggle" class="btn btn-primary" aria-label="Change theme">
                    <i class="fas fa-circle-half-stroke"></i>
                    <span id="themeLabel" class="hidden sm:inline">Theme</span>
                </button>
                <button id="settingsToggle" class="btn btn-secondary sm:hidden" aria-label="Settings">
                    <i class="fas fa-cog"></i>
                </button>
            </div>
            
            <div class="flex gap-2" id="topControls">
                <button id="copyState" class="btn btn-icon btn-secondary" title="Copy State" aria-label="Copy state">
                    <i class="fas fa-copy"></i>
                </button>
                <button id="pasteState" class="btn btn-icon btn-secondary" title="Import State" aria-label="Import state">
                    <i class="fas fa-paste"></i>
                </button>
                <button id="uploadState" class="btn btn-icon btn-secondary" title="Upload State" aria-label="Upload state">
                    <i class="fas fa-upload"></i>
                </button>
                <button id="downloadState" class="btn btn-icon btn-secondary" title="Download State" aria-label="Download state">
                    <i class="fas fa-download"></i>
                </button>
            </div>
        </header>

        <!-- Input Section with Better Mobile Layout -->
        <section class="mb-6">
            <div class="flex justify-between items-center mb-2">
                <h2 class="text-sm font-medium" style="color: var(--text-secondary);">Prompt</h2>
                <button id="copyPrompt" class="btn btn-icon btn-sm btn-secondary" title="Copy Prompt" aria-label="Copy prompt">
                    <i class="fas fa-clipboard"></i>
                </button>
            </div>
            
            <div class="flex flex-col sm:flex-row gap-2">
                <textarea 
                    id="promptInput" 
                    class="flex-1 p-3 border rounded-lg resize-none smooth-scroll"
                    style="background: var(--bg-secondary); color: var(--text-primary); border-color: var(--border);"
                    rows="3" 
                    placeholder="Enter your prompt here..."
                    aria-label="Prompt input"
                ></textarea>
                
                <div class="flex sm:flex-col gap-2">
                    <button id="attachButton" class="btn btn-secondary flex-1 sm:flex-none" aria-label="Attach files">
                        <i class="fas fa-paperclip"></i>
                        <span class="sm:hidden">Attach</span>
                    </button>
                    <button id="sendButton" class="btn btn-primary flex-1 sm:flex-none" aria-label="Send prompt">
                        <i class="fas fa-paper-plane sm:hidden"></i>
                        <span>Send</span>
                    </button>
                </div>
            </div>
            
            <!-- Hidden file inputs -->
            <input type="file" id="fileInput" multiple class="hidden" accept="*/*" />
            <input type="file" id="stateFileInput" accept=".json" class="hidden" />
            
            <!-- Selected files display -->
            <div id="selectedFiles" class="mt-3 hidden fade-in">
                <div class="text-sm font-medium mb-2" style="color: var(--text-secondary);">Selected files:</div>
                <div id="filesList" class="space-y-1"></div>
                <button id="clearFiles" class="text-xs text-red-500 hover:text-red-700 mt-2">
                    <i class="fas fa-times mr-1"></i>Clear all attachments
                </button>
            </div>
        </section>

        <!-- Response Grid -->
        <section id="responseGrid" class="response-grid mb-6" aria-label="Model responses">
            <!-- Response boxes will be generated here -->
        </section>

        <!-- Action Buttons -->
        <div class="flex justify-center gap-3 mb-6 flex-wrap">
            <button id="downloadAll" class="btn btn-primary">
                <i class="fas fa-file-download"></i>
                <span>Download All</span>
            </button>
            <button id="clearAll" class="btn btn-secondary">
                <i class="fas fa-eraser"></i>
                <span>Clear All</span>
            </button>
            <button id="randomizeAll" class="btn btn-secondary">
                <i class="fas fa-shuffle"></i>
                <span>Randomize Models</span>
            </button>
        </div>

        <!-- Preview Section -->
        <section id="previewSection" class="collapsible-section response-box mb-4">
            <div class="flex flex-wrap justify-between items-center mb-3 cursor-pointer gap-2" id="previewHeader">
                <div class="flex items-center gap-2">
                    <span class="section-arrow">▼</span>
                    <h3 class="font-medium">Preview</h3>
                </div>
                <div class="flex gap-1 flex-wrap">
                    <button class="btn btn-secondary btn-sm" data-preview="0">1</button>
                    <button class="btn btn-secondary btn-sm" data-preview="1">2</button>
                    <button class="btn btn-secondary btn-sm" data-preview="2">3</button>
                    <button class="btn btn-secondary btn-sm" data-preview="3">4</button>
                    <button id="copyPreview" class="btn btn-secondary btn-sm">
                        <i class="fas fa-copy"></i>
                    </button>
                </div>
            </div>
            <div class="section-body">
                <div id="previewContent" class="response-content smooth-scroll">
                    Select a response to preview...
                </div>
            </div>
        </section>

        <!-- Activity Log -->
        <section id="logSection" class="collapsible-section response-box">
            <div class="flex justify-between items-center mb-3 cursor-pointer" id="logHeader">
                <div class="flex items-center gap-2">
                    <span class="section-arrow">▼</span>
                    <h3 class="font-medium">Activity Log</h3>
                    <span id="logCount" class="text-xs px-2 py-1 rounded-full" style="background: var(--selected-bg); color: var(--text-secondary);">0</span>
                </div>
                <div class="flex gap-2">
                    <button id="clearLog" class="btn btn-secondary btn-sm">
                        <i class="fas fa-trash"></i>
                    </button>
                    <button id="copyLog" class="btn btn-secondary btn-sm">
                        <i class="fas fa-copy"></i>
                    </button>
                </div>
            </div>
            <div class="section-body">
                <div id="logContent" class="response-content smooth-scroll"></div>
                <div id="logEmpty" class="text-center py-6 text-sm" style="color: var(--text-secondary);">
                    No activity yet...
                </div>
            </div>
        </section>
    </div>

    <!-- Import Modal -->
    <div id="importModal" class="modal-backdrop hidden" role="dialog" aria-modal="true" aria-labelledby="importTitle">
        <div class="modal-content">
            <div class="modal-header">
                <h3 class="modal-title" id="importTitle">Import State</h3>
                <button class="modal-close" onclick="closeImportModal()" aria-label="Close modal">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            <textarea 
                id="importInput" 
                class="w-full h-64 p-3 border rounded-lg text-sm font-mono smooth-scroll"
                style="background: var(--bg-secondary); color: var(--text-primary); border-color: var(--border);"
                placeholder="Paste your JSON state here..."
                aria-label="Import JSON input"
            ></textarea>
            <div class="flex justify-end gap-3 mt-4">
                <button onclick="closeImportModal()" class="btn btn-secondary">Cancel</button>
                <button onclick="importState()" class="btn btn-primary">Load State</button>
            </div>
        </div>
    </div>

    <!-- Advanced Settings Modal -->
    <div id="advancedModal" class="modal-backdrop hidden" role="dialog" aria-modal="true" aria-labelledby="advancedTitle">
        <div class="modal-content">
            <div class="modal-header">
                <h3 class="modal-title" id="advancedTitle">Advanced Settings</h3>
                <button class="modal-close" onclick="closeAdvancedModal()" aria-label="Close modal">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            <div id="advancedContent" class="smooth-scroll max-h-96 overflow-y-auto"></div>
            <div class="flex justify-end gap-3 mt-4">
                <button onclick="closeAdvancedModal()" class="btn btn-secondary">Cancel</button>
                <button onclick="applyAdvancedSettings()" class="btn btn-primary">Apply</button>
            </div>
        </div>
    </div>

    <script>
        /* ============================================
           PERFORMANCE OPTIMIZATIONS
           ============================================ */
        
        // RequestAnimationFrame Throttle
        function rafThrottle(callback) {
            let requestId = null;
            return function(...args) {
                if (requestId === null) {
                    requestId = requestAnimationFrame(() => {
                        callback.apply(this, args);
                        requestId = null;
                    });
                }
            };
        }

        // Debounce for search input
        function debounce(func, wait) {
            let timeout;
            return function executedFunction(...args) {
                const later = () => {
                    clearTimeout(timeout);
                    func(...args);
                };
                clearTimeout(timeout);
                timeout = setTimeout(later, wait);
            };
        }

        // Intersection Observer for lazy loading
        const lazyLoadObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.remove('lazy-loading');
                    lazyLoadObserver.unobserve(entry.target);
                }
            });
        }, { rootMargin: '50px' });

        /* ============================================
           ENHANCED STATE MANAGEMENT
           ============================================ */
        
        const AppState = {
            models: {
                selected: ['', '', '', ''],
                responses: ['', '', '', ''],
                flags: [{}, {}, {}, {}]
            },
            ui: {
                accordions: [false, false, false, false],
                activeModal: null,
                theme: 'terminal',
                previewIndex: -1,
                highlightedModel: [-1, -1, -1, -1],
                isLoading: [false, false, false, false]
            },
            conversation: {
                history: [],
                attachments: []
            },
            log: [],
            advanced: {
                tempSettings: {},
                currentBox: -1
            },
            performance: {
                virtualScroll: {
                    enabled: true,
                    itemHeight: 32,
                    visibleItems: 10
                }
            }
        };

        // State persistence with fallback for canvas limitations
        function saveStateToStorage() {
            try {
                const state = {
                    models: AppState.models,
                    conversation: AppState.conversation,
                    theme: AppState.ui.theme
                };
                localStorage.setItem('aggregator-state', JSON.stringify(state));
            } catch (e) {
                // Canvas localStorage limitations - silently continue
                // No console output to avoid cluttering
            }
        }

        // Auto-save state every 30 seconds (if supported)
        let autoSaveInterval;
        try {
            autoSaveInterval = setInterval(saveStateToStorage, 30000);
        } catch (e) {
            // Silent fallback for canvas mode
        }

        // State update with persistence
        function updateState(path, value) {
            const keys = path.split('.');
            let obj = AppState;
            
            for (let i = 0; i < keys.length - 1; i++) {
                obj = obj[keys[i]];
            }
            
            const oldValue = obj[keys[keys.length - 1]];
            obj[keys[keys.length - 1]] = value;
            
            // Trigger UI updates
            syncUI(path, value, oldValue);
            
            // Save important state changes
            if (path.startsWith('models.') || path.startsWith('conversation.')) {
                saveStateToStorage();
            }
        }

        function syncUI(path, value, oldValue) {
            if (path.startsWith('models.selected')) {
                const index = parseInt(path.split('.')[2]);
                updateModelDisplay(index, value);
            } else if (path.startsWith('models.responses')) {
                const index = parseInt(path.split('.')[2]);
                updateResponseDisplay(index, value);
            } else if (path === 'ui.theme') {
                applyTheme(value);
                try {
                    localStorage.setItem('aggregator-theme', value);
                } catch (e) {
                    // Canvas mode - ignore
                }
            } else if (path.startsWith('conversation.attachments')) {
                updateAttachmentDisplay();
            } else if (path.startsWith('log')) {
                updateLogCount();
            }
        }

        /* ============================================
           COMPLETE MODEL DATABASE
           ============================================ */
        
        const MODEL_DATABASE = {
            "Favorites": [
                "@GPT-5", "@GPT-4.1", "@o3-pro", "@o1", "@Gemini-2.5-Pro",
                "@Claude-Opus-4-Reasoning", "@Claude-Opus-4.1", "@Claude-Opus-4-Search",
                "@Claude-Sonnet-4-Search", "@Grok-4", "@Mistral-Large-2"
            ],
            "Web Search": [
                "@Claude-Opus-4-Search", "@Claude-Sonnet-4-Search", "@Claude-Sonnet-3.7-Search",
                "@Gemini-2.5-Flash", "@Gemini-2.5-Pro", "@Grok-4",
                "@Perplexity-Sonar", "@Perplexity-Sonar-Rsn-Pro",
                "@GPT-4o-Search", "@Linkup-Deep-Search", "@Linkup-Standard",
                "@Bagoodex-Web-Search"
            ],
            "GPT": {
                "GPT-5 Series": ["@GPT-5-Chat", "@GPT-5", "@GPT-5-mini", "@GPT-5-nano"],
                "GPT-4 Series": [
                    "@GPT-4.1", "@GPT-4o-Search", "@GPT-4-Classic", "@GPT-4-Classic-0314",
                    "@GPT-4-Turbo", "@GPT-4.1-mini", "@GPT-4.1-nano", "@GPT-4o",
                    "@GPT-4o-mini", "@GPT-4o-mini-Search", "@GPT-4o-Aug", "@ChatGPT-4o-Latest"
                ],
                "GPT-3.5 Series": ["@GPT-3.5-Turbo", "@GPT-3.5-Turbo-Instruct", "@GPT-3.5-Turbo-Raw"],
                "o-Series": ["@o1", "@o3", "@o3-pro", "@o1-mini", "@o3-mini", "@o3-mini-high", "@o4-mini", "@o3-Pro"]
            },
            "GPT - Open Weight": {
                "120B Models": ["@GPT-OSS-120B-T", "@GPT-OSS-120B", "@GPT-OSS-120B-CS", "@OpenAI-GPT-OSS-120B"],
                "20B Models": ["@GPT-OSS-20B-T", "@GPT-OSS-20B", "@OpenAI-GPT-OSS-20B"]
            },
            "Google": {
                "Gemini 2.5": [
                    "@Gemini-2.5-Pro", "@Gemini-2.5-Flash", "@Gemini-2.5-Flash-Lite",
                    "@Gemini-2.5-Flash-Lite-Preview", "@Gemini-2.5-Flash-Image"
                ],
                "Gemini 2.0": ["@Gemini-2.0-Flash", "@Gemini-2.0-Flash-Lite", "@Gemini-2.0-Flash-Preview"],
                "Gemini 1.5": ["@Gemini-1.5-Pro", "@Gemini-1.5-Pro-Search", "@Gemini-1.5-Flash", "@Gemini-1.5-Flash-Search"],
                "Gemma": ["@Gemma-3-27B", "@Gemma-2-27b-T"]
            },
            "Claude": {
                "Opus": [
                    "@Claude-Opus-4.1", "@Claude-Opus-4", "@Claude-Opus-4-Reasoning",
                    "@Claude-Opus-4-Search", "@Claude-Opus-3"
                ],
                "Sonnet": [
                    "@Claude-Sonnet-4", "@Claude-Sonnet-4-Reasoning", "@Claude-Sonnet-4-Search",
                    "@Claude-Sonnet-3.5", "@Claude-Sonnet-3.5-June", "@Claude-Sonnet-3.5-Search",
                    "@Claude-Sonnet-3.7", "@Claude-Sonnet-3.7-Reasoning", "@Claude-Sonnet-3.7-Search"
                ],
                "Haiku": ["@Claude-Haiku-3.5", "@Claude-Haiku-3", "@Claude-Haiku-3.5-Search"]
            },
            "Grok": ["@Grok-4", "@Grok-3", "@Grok-3-Mini", "@Grok-Code-Fast-1", "@Grok-2"],
            "Llama 4": [
                "@Llama-4-Scout-B10", "@Llama-4-Maverick", "@Llama-4-Scout-T",
                "@Llama-4-Scout-CS", "@Llama-4-Scout", "@Llama-4-Maverick-T", "@Llama-4-Maverick-B10"
            ],
            "Llama 3.X": {
                "405B Models": ["@Llama-3.1-405B", "@Llama-3.1-405B-T", "@Llama-3.1-405B-FW", "@Llama-3.1-405B-FP16"],
                "70B Models": [
                    "@Llama-3-70b-Groq", "@Llama-3.3-70B", "@Llama-3.1-Nemotron",
                    "@Llama-3.3-70B-DI", "@Llama-3.3-70B-CS", "@Llama-3.3-70B-Vers",
                    "@Llama-3-70B-FP16", "@Llama-3-70B-T", "@Llama-3.1-70B",
                    "@Llama-3.1-70B-FP16", "@Llama-3.1-70B-FW", "@Llama-3.1-70B-T",
                    "@Llama-3.3-70B-FW", "@Llama-3.3-70B-N"
                ],
                "8B Models": [
                    "@Llama-3-8B-T", "@Llama-3-8b-Groq", "@Llama-3.1-8B",
                    "@Llama-3.1-8B-CS", "@Llama-3.1-8B-DI", "@Llama-3.1-8B-FP16",
                    "@Llama-3.1-8B-FW", "@Llama-3.1-8B-T-128k"
                ]
            },
            "Qwen": {
                "480B": ["@Qwen3-480B-Coder-CS", "@Qwen3-Coder-480B-T", "@Qwen3-Coder-480B-N"],
                "235B": [
                    "@Qwen-3-235B-2507-T", "@Qwen3-235B-2507-FW", "@Qwen3-235B-2507-CS",
                    "@Qwen3-235B-A22B", "@Qwen3-235B-A22B-DI", "@Qwen3-235B-A22B-N", "@Qwen3-235B-Think-CS"
                ],
                "72B": ["@Qwen-72B-T", "@Qwen2-72B-Instruct-T", "@Qwen2.5-VL-72B-T", "@Qwen-2.5-72B-T"],
                "32B": [
                    "@Qwen3-30B-A3B-Instruct", "@Qwen2.5-Coder-32B", "@Qwen2.5-Coder-32B-T",
                    "@Qwen3-32B-CS", "@Qwen3-Coder-30B-A3B"
                ],
                "Others": ["@Qwen-2.5-7B-T", "@Qwen-2.5-VL-32b", "@Qwen3-Coder"]
            },
            "DeepSeek": {
                "R1 Series": [
                    "@DeepSeek-R1", "@DeepSeek-R1-FW", "@DeepSeek-R1-DI", "@DeepSeek-R1-N",
                    "@DeepSeek-R1-Distill", "@DeepSeek-R1-Turbo-DI"
                ],
                "V3 Series": [
                    "@DeepSeek-V3.1", "@DeepSeek-V3.1-Chat", "@DeepSeek-V3.1-N",
                    "@Deepseek-V3-FW", "@DeepSeek-V3", "@DeepSeek-V3-DI", "@DeepSeek-V3-Turbo-DI"
                ],
                "Specialized": ["@DeepSeek-Prover-V2", "@DeepClaude"]
            },
            "Mistral": {
                "Large": ["@Mistral-Large-2"],
                "Medium": ["@Mistral-Medium-3", "@Mistral-Medium", "@Magistral-Medium-2506-Thinking"],
                "Small": [
                    "@Mistral-Small-3.2", "@Mistral-Small-3.1", "@Mistral-Small-3",
                    "@Mistral-7B-v0.3-DI", "@Mistral-7B-v0.3-T"
                ],
                "Specialized": ["@Mistral-NeMo", "@Mixtral8x22b-Inst-FW"]
            },
            "Perplexity": [
                "@Perplexity-R1-1776", "@Perplexity-Sonar", "@Perplexity-Sonar-Pro",
                "@Perplexity-Sonar-Rsn", "@Perplexity-Sonar-Rsn-Pro"
            ],
            "Others": [
                "@Aya-Expanse-32B", "@Aya-Vision", "@Command-R", "@Command-R-Plus",
                "@GLM-4.5", "@GLM-4.5-Air", "@GLM-4.5-Air-T", "@GLM-4.5-FW", "@GLM-4.5-Versatile",
                "@GPT-Researcher", "@Inception-Mercury", "@Inception-Mercury-Coder",
                "@Hermes-3-70B", "@Kimi-K2", "@Kimi-K2-0905-T", "@Kimi-K2-Instruct", "@Kimi-K2-T",
                "@MiniMax-M1", "@Phi-4-DI", "@QwQ-32B-B10", "@QwQ-32B-Preview-T", "@QwQ-32B-T",
                "@Reka-Core", "@Reka-Flash", "@Reka-Research", "@Tako", "@Solar-Pro-2"
            ]
        };

        // Complete flags database
        const MODEL_FLAGS = {
            "@Claude-Opus-4": { thinking_budget: { type: "slider", min: 0, max: 30768, default: "default" }},
            "@Claude-Opus-4-Reasoning": { thinking_budget: { type: "slider", min: 0, max: 30768, default: "default" }},
            "@Claude-Opus-4-Search": { thinking_budget: { type: "slider", min: 0, max: 126000, default: "default" }},
            "@Claude-Opus-4.1": { thinking_budget: { type: "slider", min: 0, max: 31999, default: "default" }},
            "@Claude-Sonnet-3.7": { thinking_budget: { type: "slider", min: 0, max: 16384, default: "default" }},
            "@Claude-Sonnet-3.7-Reasoning": { thinking_budget: { type: "slider", min: 0, max: 126000, default: "default" }},
            "@Claude-Sonnet-3.7-Search": { thinking_budget: { type: "slider", min: 0, max: 126000, default: "default" }},
            "@Claude-Sonnet-4": { thinking_budget: { type: "slider", min: 0, max: 30768, default: "default" }},
            "@Claude-Sonnet-4-Reasoning": { thinking_budget: { type: "slider", min: 0, max: 61440, default: "default" }},
            "@Claude-Sonnet-4-Search": { thinking_budget: { type: "slider", min: 0, max: 126000, default: "default" }},
            "@Gemini-2.5-Flash": { 
                thinking_budget: { type: "stepped", steps: [0, 4096, 8192, 12288, 16384, 20480, 24576], default: "default" }
            },
            "@Gemini-2.5-Flash-Lite": { 
                thinking_budget: { type: "stepped", steps: [0, 4096, 8192, 12288, 16384, 20480, 24576], default: "default" }
            },
            "@Gemini-2.5-Pro": { 
                thinking_budget: { type: "stepped", steps: [0, 4096, 8192, 12288, 16384, 20480, 24576, 28672, 32768], default: "default" }
            },
            "@GPT-5": { reasoning_effort: { type: "stepped", steps: ["minimal", "low", "medium", "high"], default: "default" }},
            "@GPT-5-mini": { reasoning_effort: { type: "stepped", steps: ["minimal", "low", "medium", "high"], default: "default" }},
            "@GPT-5-nano": { reasoning_effort: { type: "stepped", steps: ["minimal", "low", "medium", "high"], default: "default" }},
            "@o1": { reasoning_effort: { type: "stepped", steps: ["low", "medium", "high"], default: "default" }},
            "@o3": { reasoning_effort: { type: "stepped", steps: ["low", "medium", "high"], default: "default" }},
            "@o3-mini": { reasoning_effort: { type: "stepped", steps: ["low", "medium", "high"], default: "default" }},
            "@o3-pro": { reasoning_effort: { type: "stepped", steps: ["low", "medium", "high"], default: "default" }},
            "@o4-mini": { reasoning_effort: { type: "stepped", steps: ["low", "medium", "high"], default: "default" }},
            "@Grok-3-Mini": { reasoning_effort: { type: "stepped", steps: ["low", "high"], default: "default" }},
            "@Tako": { specificity: { type: "slider", min: 0, max: 100, default: "default" }},
            "@Linkup-Deep-Search": {
                advanced_settings: {
                    type: "advanced",
                    fields: {
                        domain_filter_mode: { type: "select", options: ["none", "Include", "Exclude"], default: "none" },
                        include_domains: { type: "text", placeholder: "e.g. github.com", depends_on: "domain_filter_mode", depends_value: "Include" },
                        exclude_domains: { type: "text", placeholder: "e.g. reddit.com", depends_on: "domain_filter_mode", depends_value: "Exclude" },
                        prioritize_domains: { type: "text", placeholder: "e.g. stackoverflow.com" },
                        from_date: { type: "text", placeholder: "YYYY-MM-DD" },
                        to_date: { type: "text", placeholder: "YYYY-MM-DD" },
                        include_images: { type: "checkbox", default: false },
                        image_count: { type: "text", placeholder: "Number (e.g. 5)" }
                    }
                }
            },
            "@Linkup-Standard": {
                thinking_budget: { type: "slider", min: 0, max: 32768, default: "default" },
                advanced_settings: {
                    type: "advanced",
                    fields: {
                        domain_filter_mode: { type: "select", options: ["none", "Include", "Exclude"], default: "none" },
                        include_domains: { type: "text", placeholder: "e.g. github.com", depends_on: "domain_filter_mode", depends_value: "Include" },
                        exclude_domains: { type: "text", placeholder: "e.g. reddit.com", depends_on: "domain_filter_mode", depends_value: "Exclude" },
                        prioritize_domains: { type: "text", placeholder: "e.g. stackoverflow.com" },
                        from_date: { type: "text", placeholder: "YYYY-MM-DD" },
                        to_date: { type: "text", placeholder: "YYYY-MM-DD" },
                        include_images: { type: "checkbox", default: false },
                        image_count: { type: "text", placeholder: "Number (e.g. 5)" }
                    }
                }
            },
            "@Bagoodex-Web-Search": {
                advanced_settings: {
                    type: "advanced",
                    fields: {
                        domain_filter: { type: "text", placeholder: "e.g. legal-tools.org" },
                        exclude_words: { type: "text", placeholder: "e.g. bemba" },
                        search_images: { type: "checkbox", default: false },
                        search_knowledge: { type: "checkbox", default: false },
                        search_location: { type: "checkbox", default: false },
                        search_videos: { type: "checkbox", default: false },
                        search_weather: { type: "checkbox", default: false }
                    }
                }
            }
        };

        /* ============================================
           ENHANCED UTILITY FUNCTIONS
           ============================================ */

        function showToast(message, type = 'info', duration = 2000) {
            const toast = document.createElement('div');
            toast.className = 'toast fade-in';
            toast.textContent = message;
            toast.setAttribute('role', 'alert');
            document.body.appendChild(toast);
            
            setTimeout(() => {
                toast.style.animation = 'slideIn 0.3s ease reverse';
                setTimeout(() => document.body.removeChild(toast), 300);
            }, duration);
        }

        function getTimestamp() {
            const now = new Date();
            return now.toISOString().replace(/[:.]/g, '-').slice(0, -5);
        }

        async function copyToClipboard(text) {
            try {
                await navigator.clipboard.writeText(text);
                showToast('Copied to clipboard!');
            } catch (err) {
                // Fallback for older browsers
                const textarea = document.createElement('textarea');
                textarea.value = text;
                textarea.style.position = 'fixed';
                textarea.style.opacity = '0';
                document.body.appendChild(textarea);
                textarea.select();
                try {
                    document.execCommand('copy');
                    showToast('Copied to clipboard!');
                } catch (e) {
                    showToast('Failed to copy', 'error');
                }
                document.body.removeChild(textarea);
            }
        }

        function downloadJSON(data, filename) {
            const blob = new Blob([JSON.stringify(data, null, 2)], { type: 'application/json' });
            const url = URL.createObjectURL(blob);
            const a = document.createElement('a');
            a.href = url;
            a.download = filename;
            a.click();
            URL.revokeObjectURL(url);
        }

        function downloadText(text, filename) {
            const blob = new Blob([text], { type: 'text/plain' });
            const url = URL.createObjectURL(blob);
            const a = document.createElement('a');
            a.href = url;
            a.download = filename;
            a.click();
            URL.revokeObjectURL(url);
        }

        function formatFileSize(bytes) {
            if (bytes === 0) return '0 Bytes';
            const k = 1024;
            const sizes = ['Bytes', 'KB', 'MB', 'GB'];
            const i = Math.floor(Math.log(bytes) / Math.log(k));
            return parseFloat((bytes / Math.pow(k, i)).toFixed(2)) + ' ' + sizes[i];
        }

        /* ============================================
           THEME SYSTEM WITH TRANSITIONS
           ============================================ */

        const THEMES = ['default', 'dark', 'terminal', 'paper', 'sakura', 'ayu'];
        
        function applyTheme(themeName) {
            document.body.style.transition = 'none';
            
            if (themeName === 'default') {
                document.documentElement.removeAttribute('data-theme');
            } else {
                document.documentElement.setAttribute('data-theme', themeName);
            }
            
            const label = document.getElementById('themeLabel');
            if (label) {
                const displayName = themeName.charAt(0).toUpperCase() + themeName.slice(1);
                label.textContent = window.innerWidth > 640 ? displayName : '';
            }
            
            // Update meta theme-color for mobile browsers
            const metaTheme = document.querySelector('meta[name="theme-color"]');
            if (metaTheme) {
                const themeColor = getComputedStyle(document.documentElement).getPropertyValue('--accent').trim();
                metaTheme.content = themeColor;
            }
            
            requestAnimationFrame(() => {
                document.body.style.transition = '';
            });
        }

        function cycleTheme() {
            const currentIndex = THEMES.indexOf(AppState.ui.theme);
            const nextIndex = (currentIndex + 1) % THEMES.length;
            updateState('ui.theme', THEMES[nextIndex]);
        }

        /* ============================================
           OPTIMIZED RESPONSE BOX GENERATION
           ============================================ */

        function createResponseBox(index) {
            const box = document.createElement('div');
            box.className = 'response-box fade-in';
            box.dataset.index = index;
            
            box.innerHTML = `
                <div class="mb-3">
                    <div id="flags-${index}" class="flag-control hidden"></div>
                    <div class="flex gap-2 items-center">
                        <div class="accordion-container collapsed flex-1" id="selector-${index}">
                            <div class="selected-display" onclick="toggleAccordion(${index})" style="cursor: pointer;">
                                <input 
                                    id="search-${index}" 
                                    type="text" 
                                    placeholder="Search or select model..."
                                    onclick="event.stopPropagation()"
                                    onFocus="if(document.getElementById('selector-${index}').classList.contains('collapsed')) toggleAccordion(${index})"
                                    aria-label="Search models for box ${index + 1}"
                                    style="cursor: text; flex: 1;"
                                />
                                <button id="clear-${index}" class="btn btn-sm btn-secondary hidden" onclick="clearModel(event, ${index})" aria-label="Clear selection" style="margin-right: 8px;">
                                    Clear
                                </button>
                                <i class="fas fa-chevron-down text-sm transition-transform" style="min-width: 16px; color: var(--text-secondary);"></i>
                            </div>
                            <div class="accordion-wrapper smooth-scroll" id="accordion-${index}"></div>
                        </div>
                        <button onclick="selectRandomModel(${index})" class="btn btn-icon btn-sm btn-secondary" title="Random" aria-label="Select random model">
                            <i class="fas fa-random"></i>
                        </button>
                        <div id="status-${index}" class="status-indicator hidden" aria-live="polite">
                            <div class="status-spinner hidden"></div>
                            <i class="fas fa-check hidden" aria-label="Success"></i>
                            <i class="fas fa-times hidden" aria-label="Error"></i>
                        </div>
                    </div>
                </div>
                <div class="response-content smooth-scroll" id="response-${index}" aria-live="polite"></div>
                <div class="flex gap-1 mt-2 flex-wrap">
                    <button onclick="previewResponse(${index})" class="btn btn-secondary btn-sm" title="Preview" aria-label="Preview response">
                        <i class="fas fa-eye"></i>
                    </button>
                    <button onclick="copyResponse(${index})" class="btn btn-secondary btn-sm" title="Copy" aria-label="Copy response">
                        <i class="fas fa-copy"></i>
                    </button>
                    <button onclick="downloadResponse(${index})" class="btn btn-secondary btn-sm" title="Download" aria-label="Download response">
                        <i class="fas fa-download"></i>
                    </button>
                    <button onclick="clearResponse(${index})" class="btn btn-secondary btn-sm" title="Clear" aria-label="Clear response">
                        <i class="fas fa-eraser"></i>
                    </button>
                </div>
            `;
            
            return box;
        }

        function initializeResponseBoxes() {
            const grid = document.getElementById('responseGrid');
            for (let i = 0; i < 4; i++) {
                grid.appendChild(createResponseBox(i));
                initializeAccordion(i);
            }
        }

        /* ============================================
           VIRTUAL SCROLL ACCORDION SYSTEM
           ============================================ */

        function initializeAccordion(index) {
            // Force render accordion content immediately
            setTimeout(() => {
                renderAccordionContent(index);
            }, 100);
            
            const searchInput = document.getElementById(`search-${index}`);
            if (!searchInput) {
                console.error(`Search input not found for index ${index}`);
                return;
            }
            
            // Debounced search
            const debouncedSearch = debounce((term) => {
                filterModels(index, term);
            }, 150);
            
            searchInput.addEventListener('input', (e) => {
                debouncedSearch(e.target.value.toLowerCase());
            });
            
            // Keyboard navigation
            searchInput.addEventListener('keydown', (e) => {
                if (e.key === 'Escape') {
                    e.target.value = '';
                    filterModels(index, '');
                } else if (e.key === 'ArrowDown' || e.key === 'ArrowUp') {
                    e.preventDefault();
                    navigateModels(index, e.key === 'ArrowDown' ? 1 : -1);
                } else if (e.key === 'Enter') {
                    e.preventDefault();
                    selectHighlightedModel(index);
                }
            });
        }

        function renderAccordionContent(index, filter = '') {
            const wrapper = document.getElementById(`accordion-${index}`);
            let html = '';
            let totalModels = 0;
            
            Object.entries(MODEL_DATABASE).forEach(([category, models]) => {
                const filteredModels = filterModelList(models, filter);
                if (filteredModels.length === 0 && filter) return;
                
                totalModels += filteredModels.length;
                
                html += `<div class="accordion-section">
                    <div class="accordion-header" onclick="toggleSection(this)" role="button" aria-expanded="false">
                        <span>${category}</span>
                        <span class="text-xs">(${filteredModels.length})</span>
                    </div>
                    <div class="accordion-content" role="region">`;
                
                if (typeof models === 'object' && !Array.isArray(models)) {
                    Object.entries(models).forEach(([subcat, submodels]) => {
                        const filteredSub = filterModelList(submodels, filter);
                        if (filteredSub.length === 0 && filter) return;
                        
                        html += `<div class="sub-section">
                            <div class="sub-header">${subcat}</div>`;
                        
                        // Virtual scrolling for large lists
                        if (filteredSub.length > 50) {
                            html += `<div class="virtual-scroll-container" style="height: 200px;" data-models='${JSON.stringify(filteredSub)}' data-index="${index}">
                                <div class="virtual-scroll-viewport"></div>
                            </div>`;
                        } else {
                            filteredSub.forEach(model => {
                                html += createModelItem(model, index);
                            });
                        }
                        html += `</div>`;
                    });
                } else {
                    if (filteredModels.length > 50) {
                        html += `<div class="virtual-scroll-container" style="height: 200px;" data-models='${JSON.stringify(filteredModels)}' data-index="${index}">
                            <div class="virtual-scroll-viewport"></div>
                        </div>`;
                    } else {
                        filteredModels.forEach(model => {
                            html += createModelItem(model, index);
                        });
                    }
                }
                
                html += `</div></div>`;
            });
            
            wrapper.innerHTML = html || '<div class="text-center py-4 text-sm" style="color: var(--text-secondary);">No models found</div>';
            
            // Initialize virtual scrolling
            wrapper.querySelectorAll('.virtual-scroll-container').forEach(container => {
                initializeVirtualScroll(container);
            });
            
            // Auto-expand sections when filtering
            if (filter) {
                wrapper.querySelectorAll('.accordion-header').forEach(h => {
                    h.classList.add('active');
                    h.setAttribute('aria-expanded', 'true');
                });
                wrapper.querySelectorAll('.accordion-content').forEach(c => c.classList.add('active'));
            }
            
            // Restore selection
            const selected = AppState.models.selected[index];
            if (selected) {
                const item = wrapper.querySelector(`[data-model="${selected}"]`);
                if (item) item.classList.add('selected');
            }
        }

        function initializeVirtualScroll(container) {
            const models = JSON.parse(container.dataset.models);
            const index = parseInt(container.dataset.index);
            const itemHeight = 32;
            const visibleItems = Math.ceil(200 / itemHeight);
            const viewport = container.querySelector('.virtual-scroll-viewport');
            
            let scrollTop = 0;
            let startIndex = 0;
            
            function render() {
                const endIndex = Math.min(startIndex + visibleItems + 2, models.length);
                let html = '';
                
                for (let i = startIndex; i < endIndex; i++) {
                    html += `<div style="position: absolute; top: ${i * itemHeight}px; left: 0; right: 0;">
                        ${createModelItem(models[i], index)}
                    </div>`;
                }
                
                viewport.innerHTML = html;
                viewport.style.height = `${models.length * itemHeight}px`;
            }
            
            container.addEventListener('scroll', rafThrottle(() => {
                scrollTop = container.scrollTop;
                startIndex = Math.floor(scrollTop / itemHeight);
                render();
            }));
            
            render();
        }

        function createModelItem(model, index) {
            const selected = AppState.models.selected[index] === model;
            const highlighted = AppState.ui.highlightedModel[index] === model;
            return `<div class="model-item ${selected ? 'selected' : ''} ${highlighted ? 'highlighted' : ''}" 
                        onclick="selectModel(${index}, '${model}')"
                        data-model="${model}"
                        role="option"
                        aria-selected="${selected}">
                        ${model}
                    </div>`;
        }

        function filterModelList(models, term) {
            if (!term) return Array.isArray(models) ? models : [];
            
            const lowerTerm = term.toLowerCase();
            if (Array.isArray(models)) {
                return models.filter(m => m.toLowerCase().includes(lowerTerm));
            } else {
                const result = [];
                Object.values(models).forEach(submodels => {
                    result.push(...submodels.filter(m => m.toLowerCase().includes(lowerTerm)));
                });
                return result;
            }
        }

        /* ============================================
           COMPLETE EVENT HANDLERS & API
           ============================================ */

        // All remaining functions from Part 2...
        function toggleAccordion(index) {
            const container = document.getElementById(`selector-${index}`);
            const isCollapsed = container.classList.contains('collapsed');
            
            // Close all other accordions
            for (let i = 0; i < 4; i++) {
                if (i !== index) {
                    document.getElementById(`selector-${i}`).classList.add('collapsed');
                }
            }
            
            if (isCollapsed) {
                container.classList.remove('collapsed');
                document.getElementById(`search-${index}`).focus();
            } else {
                container.classList.add('collapsed');
            }
        }

        function toggleSection(header) {
            const isActive = header.classList.contains('active');
            const content = header.nextElementSibling;
            
            if (isActive) {
                header.classList.remove('active');
                content.classList.remove('active');
                header.setAttribute('aria-expanded', 'false');
            } else {
                header.classList.add('active');
                content.classList.add('active');
                header.setAttribute('aria-expanded', 'true');
            }
        }

        function selectModel(index, model) {
            updateState(`models.selected.${index}`, model);
            toggleAccordion(index);
            updateFlagControls(index, model);
        }

        function clearModel(event, index) {
            event.stopPropagation();
            updateState(`models.selected.${index}`, '');
            document.getElementById(`search-${index}`).value = '';
            document.getElementById(`clear-${index}`).classList.add('hidden');
            updateFlagControls(index, null);
            renderAccordionContent(index);
        }

        function selectRandomModel(index) {
            const allModels = [];
            Object.values(MODEL_DATABASE).forEach(category => {
                if (Array.isArray(category)) {
                    allModels.push(...category);
                } else {
                    Object.values(category).forEach(models => {
                        allModels.push(...models);
                    });
                }
            });
            
            const randomModel = allModels[Math.floor(Math.random() * allModels.length)];
            selectModel(index, randomModel);
            showToast(`Selected: ${randomModel}`);
        }

        function randomizeAllModels() {
            for (let i = 0; i < 4; i++) {
                selectRandomModel(i);
            }
        }

        function clearAllResponses() {
            for (let i = 0; i < 4; i++) {
                updateState(`models.responses.${i}`, '');
            }
            showToast('All responses cleared');
        }

        function updateModelDisplay(index, model) {
            const searchInput = document.getElementById(`search-${index}`);
            const clearBtn = document.getElementById(`clear-${index}`);
            
            searchInput.value = model;
            if (model) {
                clearBtn.classList.remove('hidden');
            } else {
                clearBtn.classList.add('hidden');
            }
            
            // Update visual selection
            const wrapper = document.getElementById(`accordion-${index}`);
            wrapper.querySelectorAll('.model-item').forEach(item => {
                if (item.dataset.model === model) {
                    item.classList.add('selected');
                    item.setAttribute('aria-selected', 'true');
                } else {
                    item.classList.remove('selected');
                    item.setAttribute('aria-selected', 'false');
                }
            });
        }

        function filterModels(index, term) {
            renderAccordionContent(index, term);
            
            // Auto-expand accordion when searching
            if (term && document.getElementById(`selector-${index}`).classList.contains('collapsed')) {
                toggleAccordion(index);
            }
        }

        function navigateModels(index, direction) {
            const wrapper = document.getElementById(`accordion-${index}`);
            const items = Array.from(wrapper.querySelectorAll('.model-item'));
            if (items.length === 0) return;
            
            let currentIndex = items.findIndex(item => item.classList.contains('highlighted'));
            if (currentIndex === -1) {
                currentIndex = direction === 1 ? -1 : items.length;
            }
            
            items.forEach(item => item.classList.remove('highlighted'));
            
            currentIndex += direction;
            if (currentIndex < 0) currentIndex = items.length - 1;
            if (currentIndex >= items.length) currentIndex = 0;
            
            items[currentIndex].classList.add('highlighted');
            items[currentIndex].scrollIntoView({ block: 'nearest', behavior: 'smooth' });
            
            updateState(`ui.highlightedModel.${index}`, items[currentIndex].dataset.model);
        }

        function selectHighlightedModel(index) {
            const wrapper = document.getElementById(`accordion-${index}`);
            const highlighted = wrapper.querySelector('.model-item.highlighted');
            if (highlighted) {
                selectModel(index, highlighted.dataset.model);
            }
        }

        // Flag Controls (same as Part 2)
        function updateFlagControls(index, model) {
            const container = document.getElementById(`flags-${index}`);
            
            if (!model || !MODEL_FLAGS[model]) {
                container.classList.add('hidden');
                container.innerHTML = '';
                AppState.models.flags[index] = {};
                return;
            }
            
            container.classList.remove('hidden');
            container.innerHTML = '';
            
            const flags = MODEL_FLAGS[model];
            
            Object.entries(flags).forEach(([flagName, config]) => {
                if (config.type === 'slider') {
                    createSliderFlag(container, index, flagName, config);
                } else if (config.type === 'stepped') {
                    createSteppedFlag(container, index, flagName, config);
                } else if (config.type === 'advanced') {
                    createAdvancedButton(container, index, flagName, config);
                }
            });
        }

        function createSliderFlag(container, boxIndex, flagName, config) {
            const div = document.createElement('div');
            div.className = 'mb-3';
            
            const label = document.createElement('label');
            label.className = 'flag-label';
            label.textContent = flagName.replace(/_/g, ' ');
            
            const sliderContainer = document.createElement('div');
            sliderContainer.className = 'flex items-center gap-2';
            
            const slider = document.createElement('input');
            slider.type = 'range';
            slider.min = config.min;
            slider.max = config.max;
            slider.value = config.min;
            slider.className = 'flag-slider flex-1';
            slider.setAttribute('aria-label', flagName);
            
            const valueDisplay = document.createElement('span');
            valueDisplay.className = 'flag-value';
            valueDisplay.textContent = 'default';
            
            if (!AppState.models.flags[boxIndex][flagName]) {
                AppState.models.flags[boxIndex][flagName] = config.default;
            }
            
            slider.addEventListener('input', (e) => {
                const value = parseInt(e.target.value);
                valueDisplay.textContent = value === config.min ? 'default' : value;
                AppState.models.flags[boxIndex][flagName] = value === config.min ? 'default' : value;
            });
            
            sliderContainer.appendChild(slider);
            sliderContainer.appendChild(valueDisplay);
            div.appendChild(label);
            div.appendChild(sliderContainer);
            container.appendChild(div);
        }

        function createSteppedFlag(container, boxIndex, flagName, config) {
            const div = document.createElement('div');
            div.className = 'mb-3';
            
            const label = document.createElement('label');
            label.className = 'flag-label';
            label.textContent = flagName.replace(/_/g, ' ');
            
            const sliderContainer = document.createElement('div');
            sliderContainer.className = 'flex items-center gap-2';
            
            const slider = document.createElement('input');
            slider.type = 'range';
            slider.min = 0;
            slider.max = config.steps.length;
            slider.value = 0;
            slider.step = 1;
            slider.className = 'flag-slider flex-1';
            slider.setAttribute('aria-label', flagName);
            
            const valueDisplay = document.createElement('span');
            valueDisplay.className = 'flag-value';
            valueDisplay.textContent = 'default';
            
            if (!AppState.models.flags[boxIndex][flagName]) {
                AppState.models.flags[boxIndex][flagName] = config.default;
            }
            
            slider.addEventListener('input', (e) => {
                const position = parseInt(e.target.value);
                if (position === 0) {
                    valueDisplay.textContent = 'default';
                    AppState.models.flags[boxIndex][flagName] = 'default';
                } else {
                    const value = config.steps[position - 1];
                    valueDisplay.textContent = value;
                    AppState.models.flags[boxIndex][flagName] = value;
                }
            });
            
            sliderContainer.appendChild(slider);
            sliderContainer.appendChild(valueDisplay);
            div.appendChild(label);
            div.appendChild(sliderContainer);
            container.appendChild(div);
        }

        function createAdvancedButton(container, boxIndex, flagName, config) {
            const button = document.createElement('button');
            button.className = 'btn btn-primary btn-sm w-full';
            button.innerHTML = '<i class="fas fa-cog"></i> Advanced Settings';
            button.onclick = () => openAdvancedModal(boxIndex, flagName, config);
            container.appendChild(button);
        }

        // Advanced Modal (same as Part 2)
        function openAdvancedModal(boxIndex, flagName, config) {
            AppState.advanced.currentBox = boxIndex;
            AppState.advanced.tempSettings = {};
            
            const modal = document.getElementById('advancedModal');
            const title = document.getElementById('advancedTitle');
            const content = document.getElementById('advancedContent');
            
            title.textContent = `Advanced Settings - ${AppState.models.selected[boxIndex]}`;
            content.innerHTML = '';
            
            Object.entries(config.fields).forEach(([fieldName, fieldConfig]) => {
                const fieldDiv = document.createElement('div');
                fieldDiv.className = 'mb-4';
                fieldDiv.id = `field-${fieldName}`;
                
                const label = document.createElement('label');
                label.className = 'flag-label';
                label.textContent = fieldName.replace(/_/g, ' ');
                fieldDiv.appendChild(label);
                
                if (fieldConfig.type === 'select') {
                    const select = document.createElement('select');
                    select.className = 'w-full p-2 border rounded';
                    select.style.cssText = 'background: var(--bg-secondary); color: var(--text-primary); border-color: var(--border);';
                    
                    fieldConfig.options.forEach(option => {
                        const opt = document.createElement('option');
                        opt.value = option;
                        opt.textContent = option;
                        select.appendChild(opt);
                    });
                    
                    select.value = fieldConfig.default || fieldConfig.options[0];
                    select.onchange = (e) => {
                        AppState.advanced.tempSettings[fieldName] = e.target.value;
                        updateFieldVisibility(config.fields);
                    };
                    
                    fieldDiv.appendChild(select);
                } else if (fieldConfig.type === 'text') {
                    const input = document.createElement('input');
                    input.type = 'text';
                    input.className = 'w-full p-2 border rounded';
                    input.style.cssText = 'background: var(--bg-secondary); color: var(--text-primary); border-color: var(--border);';
                    input.placeholder = fieldConfig.placeholder || '';
                    input.oninput = (e) => {
                        AppState.advanced.tempSettings[fieldName] = e.target.value;
                    };
                    fieldDiv.appendChild(input);
                } else if (fieldConfig.type === 'checkbox') {
                    const checkContainer = document.createElement('div');
                    checkContainer.className = 'flex items-center gap-2';
                    
                    const checkbox = document.createElement('input');
                    checkbox.type = 'checkbox';
                    checkbox.id = `check-${fieldName}`;
                    checkbox.checked = fieldConfig.default || false;
                    checkbox.onchange = (e) => {
                        AppState.advanced.tempSettings[fieldName] = e.target.checked;
                    };
                    
                    const checkLabel = document.createElement('label');
                    checkLabel.htmlFor = `check-${fieldName}`;
                    checkLabel.textContent = `Enable ${fieldName.replace(/_/g, ' ')}`;
                    checkLabel.style.cssText = 'color: var(--text-secondary); font-size: 12px;';
                    
                    checkContainer.appendChild(checkbox);
                    checkContainer.appendChild(checkLabel);
                    fieldDiv.appendChild(checkContainer);
                }
                
                content.appendChild(fieldDiv);
            });
            
            updateFieldVisibility(config.fields);
            modal.classList.remove('hidden');
        }

        function updateFieldVisibility(fields) {
            Object.entries(fields).forEach(([fieldName, fieldConfig]) => {
                const field = document.getElementById(`field-${fieldName}`);
                if (fieldConfig.depends_on && fieldConfig.depends_value) {
                    const dependsValue = AppState.advanced.tempSettings[fieldConfig.depends_on];
                    field.style.display = dependsValue === fieldConfig.depends_value ? 'block' : 'none';
                }
            });
        }

        function closeAdvancedModal() {
            document.getElementById('advancedModal').classList.add('hidden');
        }

        function applyAdvancedSettings() {
            const boxIndex = AppState.advanced.currentBox;
            AppState.models.flags[boxIndex].advanced_settings = { ...AppState.advanced.tempSettings };
            closeAdvancedModal();
            showToast('Advanced settings applied!');
        }

        // Poe API Integration with fallback
        if (window.Poe) {
            window.Poe.registerHandler('response-handler', (result, context) => {
                const { boxIndex } = context;
                const msg = result.responses[0];
                
                if (msg.status === 'error') {
                    updateState(`models.responses.${boxIndex}`, 'Error: ' + (msg.statusText || 'Unknown error'));
                    updateStatusIndicator(boxIndex, 'error');
                    addLogEntry('error', `Box ${boxIndex + 1}`, msg.statusText || 'Unknown error');
                } else if (msg.status === 'incomplete') {
                    updateStatusIndicator(boxIndex, 'loading');
                    updateState(`models.responses.${boxIndex}`, msg.content || 'Generating...');
                } else if (msg.status === 'complete') {
                    updateState(`models.responses.${boxIndex}`, msg.content);
                    updateStatusIndicator(boxIndex, 'success');
                    addLogEntry('response', `Box ${boxIndex + 1} (${AppState.models.selected[boxIndex]})`, msg.content);
                }
            });
        } else {
            console.log('Poe API not available - Demo mode');
        }

        async function sendMessages() {
            const prompt = document.getElementById('promptInput').value.trim();
            if (!prompt) {
                showToast('Please enter a prompt');
                return;
            }
            
            AppState.conversation.history.push(prompt);
            
            let contextPrompt = prompt;
            if (AppState.conversation.history.length > 1) {
                const previousPrompts = AppState.conversation.history.slice(0, -1);
                contextPrompt = `=== Previous Conversation ===\n${previousPrompts.map((p, i) => `${i + 1}. ${p}`).join('\n')}\n\n=== Current Prompt ===\n${prompt}`;
            }
            
            addLogEntry('user', 'User Prompt', prompt);
            if (AppState.conversation.attachments.length > 0) {
                addLogEntry('user', 'Attachments', `${AppState.conversation.attachments.length} file(s)`);
            }
            
            for (let i = 0; i < 4; i++) {
                const model = AppState.models.selected[i];
                if (!model) continue;
                
                updateStatusIndicator(i, 'loading');
                updateState(`models.responses.${i}`, 'Sending...');
                
                let flagsString = '';
                const flags = AppState.models.flags[i];
                if (flags && Object.keys(flags).length > 0) {
                    const flagParts = [];
                    
                    Object.entries(flags).forEach(([flagName, value]) => {
                        if (flagName === 'advanced_settings' && typeof value === 'object') {
                            Object.entries(value).forEach(([subFlag, subValue]) => {
                                if (subValue && subValue !== 'none' && subValue !== false) {
                                    if (typeof subValue === 'boolean') {
                                        flagParts.push(`--${subFlag} true`);
                                    } else {
                                        const isLinkup = model.includes('Linkup');
                                        if (isLinkup) {
                                            flagParts.push(`--${subFlag} ${subValue}`);
                                        } else {
                                            flagParts.push(`--${subFlag} "${subValue}"`);
                                        }
                                    }
                                }
                            });
                        } else if (value !== 'default') {
                            flagParts.push(`--${flagName} ${value}`);
                        }
                    });
                    
                    if (flagParts.length > 0) {
                        flagsString = ' ' + flagParts.join(' ');
                    }
                }
                
                const fullPrompt = `${model} ${contextPrompt}${flagsString}`;
                
                addLogEntry('request', `Box ${i + 1} (${model})`, fullPrompt);
                
                try {
                    await window.Poe.sendUserMessage(fullPrompt, {
                        handler: 'response-handler',
                        stream: true,
                        openChat: false,
                        handlerContext: { boxIndex: i },
                        attachments: AppState.conversation.attachments.length > 0 ? AppState.conversation.attachments : undefined
                    });
                } catch (err) {
                    console.error('SendUserMessage error:', err);
                    updateState(`models.responses.${i}`, 'Error: ' + err.message);
                    updateStatusIndicator(i, 'error');
                    addLogEntry('error', `Box ${i + 1}`, err.message);
                }
            }
        }

        function updateStatusIndicator(boxIndex, state) {
            const indicator = document.getElementById(`status-${boxIndex}`);
            const spinner = indicator.querySelector('.status-spinner');
            const checkIcon = indicator.querySelector('.fa-check');
            const timesIcon = indicator.querySelector('.fa-times');
            
            indicator.classList.remove('hidden', 'loading', 'success', 'error');
            spinner.classList.add('hidden');
            checkIcon.classList.add('hidden');
            timesIcon.classList.add('hidden');
            
            if (state === 'loading') {
                indicator.classList.add('loading');
                spinner.classList.remove('hidden');
            } else if (state === 'success') {
                indicator.classList.add('success');
                checkIcon.classList.remove('hidden');
                setTimeout(() => {
                    indicator.classList.add('hidden');
                }, 3000);
            } else if (state === 'error') {
                indicator.classList.add('error');
                timesIcon.classList.remove('hidden');
            } else {
                indicator.classList.add('hidden');
            }
        }

        // File Attachments
        function updateAttachmentDisplay() {
            const container = document.getElementById('selectedFiles');
            const filesList = document.getElementById('filesList');
            
            if (AppState.conversation.attachments.length === 0) {
                container.classList.add('hidden');
                return;
            }
            
            container.classList.remove('hidden');
            filesList.innerHTML = '';
            
            AppState.conversation.attachments.forEach((file, index) => {
                const fileItem = document.createElement('div');
                fileItem.className = 'file-item';
                
                const fileInfo = document.createElement('div');
                fileInfo.className = 'file-info';
                
                const icon = document.createElement('i');
                icon.className = getFileIcon(file.type);
                
                const fileName = document.createElement('span');
                fileName.className = 'file-name';
                fileName.textContent = file.name;
                
                const fileSize = document.createElement('span');
                fileSize.className = 'file-size';
                fileSize.textContent = formatFileSize(file.size);
                
                fileInfo.appendChild(icon);
                fileInfo.appendChild(fileName);
                fileInfo.appendChild(fileSize);
                
                const removeBtn = document.createElement('span');
                removeBtn.className = 'file-remove';
                removeBtn.innerHTML = '<i class="fas fa-times"></i>';
                removeBtn.onclick = () => removeAttachment(index);
                
                fileItem.appendChild(fileInfo);
                fileItem.appendChild(removeBtn);
                filesList.appendChild(fileItem);
            });
        }

        function getFileIcon(type) {
            if (type.startsWith('image/')) return 'fas fa-image text-blue-500';
            if (type.startsWith('video/')) return 'fas fa-video text-purple-500';
            if (type.startsWith('audio/')) return 'fas fa-music text-green-500';
            if (type.includes('pdf')) return 'fas fa-file-pdf text-red-500';
            if (type.includes('text')) return 'fas fa-file-text text-gray-600';
            return 'fas fa-file text-gray-500';
        }

        function removeAttachment(index) {
            AppState.conversation.attachments.splice(index, 1);
            updateAttachmentDisplay();
            showToast('File removed');
        }

        // Activity Log
        function addLogEntry(type, title, content) {
            const entry = {
                timestamp: new Date().toLocaleString(),
                type,
                title,
                content
            };
            
            AppState.log.push(entry);
            renderLogEntry(entry);
            updateLogCount();
        }

        function renderLogEntry(entry) {
            const logContent = document.getElementById('logContent');
            const logEmpty = document.getElementById('logEmpty');
            
            if (logEmpty) logEmpty.style.display = 'none';
            
            const div = document.createElement('div');
            div.className = `log-entry ${entry.type} fade-in`;
            
            div.innerHTML = `
                <div class="log-timestamp">${entry.timestamp}</div>
                <div class="log-title">${entry.title}</div>
                <div class="log-content">${entry.content.substring(0, 200)}${entry.content.length > 200 ? '...' : ''}</div>
            `;
            
            logContent.appendChild(div);
            logContent.scrollTop = logContent.scrollHeight;
        }

        function updateLogCount() {
            const count = document.getElementById('logCount');
            if (count) count.textContent = AppState.log.length;
        }

        function clearLog() {
            AppState.log = [];
            document.getElementById('logContent').innerHTML = '';
            document.getElementById('logEmpty').style.display = 'block';
            updateLogCount();
            showToast('Log cleared');
        }

        // Response Actions
        function updateResponseDisplay(index, content) {
            const element = document.getElementById(`response-${index}`);
            element.textContent = content;
            
            // Add skeleton loading effect for empty responses
            if (!content) {
                element.classList.add('skeleton');
            } else {
                element.classList.remove('skeleton');
            }
        }

        function previewResponse(index) {
            const content = AppState.models.responses[index] || 'No response yet';
            document.getElementById('previewContent').textContent = content;
            showToast(`Previewing response ${index + 1}`);
        }

        function copyResponse(index) {
            const content = AppState.models.responses[index];
            if (content) {
                copyToClipboard(content);
            } else {
                showToast('No content to copy');
            }
        }

        function downloadResponse(index) {
            const content = AppState.models.responses[index];
            if (content) {
                downloadText(content, `response_${index + 1}_${getTimestamp()}.txt`);
            } else {
                showToast('No content to download');
            }
        }

        function clearResponse(index) {
            updateState(`models.responses.${index}`, '');
            showToast('Response cleared');
        }

        // State Import/Export
        function exportState() {
            const state = {
                models: AppState.models,
                conversation: AppState.conversation,
                timestamp: new Date().toISOString(),
                version: '3.0'
            };
            return state;
        }

        function importState() {
            const input = document.getElementById('importInput').value.trim();
            if (!input) {
                showToast('Please paste JSON state');
                return;
            }
            
            try {
                const state = JSON.parse(input);
                
                if (state.models) {
                    AppState.models = state.models;
                    for (let i = 0; i < 4; i++) {
                        updateModelDisplay(i, AppState.models.selected[i]);
                        updateResponseDisplay(i, AppState.models.responses[i]);
                        updateFlagControls(i, AppState.models.selected[i]);
                    }
                }
                
                if (state.conversation) {
                    AppState.conversation = state.conversation;
                    updateAttachmentDisplay();
                }
                
                closeImportModal();
                showToast('State imported successfully!');
                saveStateToStorage();
            } catch (err) {
                showToast('Invalid JSON state', 'error');
            }
        }

        function closeImportModal() {
            document.getElementById('importModal').classList.add('hidden');
            document.getElementById('importInput').value = '';
        }

        /* ============================================
           INITIALIZATION
           ============================================ */

        document.addEventListener('DOMContentLoaded', () => {
            // Load saved state (silent fallback for canvas)
            try {
                const savedState = localStorage.getItem('aggregator-state');
                if (savedState) {
                    const state = JSON.parse(savedState);
                    if (state.models) AppState.models = state.models;
                    if (state.conversation) AppState.conversation = state.conversation;
                    if (state.theme) AppState.ui.theme = state.theme;
                }
            } catch (e) {
                // Canvas mode - no localStorage access
            }
            
            initializeResponseBoxes();
            applyTheme(AppState.ui.theme);
            
            // Update displays for loaded state
            for (let i = 0; i < 4; i++) {
                if (AppState.models.selected[i]) {
                    updateModelDisplay(i, AppState.models.selected[i]);
                    updateFlagControls(i, AppState.models.selected[i]);
                }
                if (AppState.models.responses[i]) {
                    updateResponseDisplay(i, AppState.models.responses[i]);
                }
            }
            
            // Theme toggle
            document.getElementById('themeToggle').addEventListener('click', cycleTheme);
            
            // Send button
            document.getElementById('sendButton').addEventListener('click', sendMessages);
            
            // Enter to send
            document.getElementById('promptInput').addEventListener('keydown', (e) => {
                if (e.key === 'Enter' && e.ctrlKey) {
                    sendMessages();
                }
            });
            
            // Copy prompt
            document.getElementById('copyPrompt').addEventListener('click', () => {
                const prompt = document.getElementById('promptInput').value;
                if (prompt) {
                    copyToClipboard(prompt);
                } else {
                    showToast('No prompt to copy');
                }
            });
            
            // Attachments
            document.getElementById('attachButton').addEventListener('click', () => {
                document.getElementById('fileInput').click();
            });
            
            document.getElementById('fileInput').addEventListener('change', (e) => {
                const files = Array.from(e.target.files);
                AppState.conversation.attachments.push(...files);
                updateAttachmentDisplay();
                showToast(`${files.length} file(s) attached`);
                e.target.value = '';
            });
            
            document.getElementById('clearFiles').addEventListener('click', () => {
                AppState.conversation.attachments = [];
                updateAttachmentDisplay();
                showToast('All attachments cleared');
            });
            
            // State management
            document.getElementById('copyState').addEventListener('click', () => {
                const state = exportState();
                copyToClipboard(JSON.stringify(state, null, 2));
            });
            
            document.getElementById('pasteState').addEventListener('click', () => {
                document.getElementById('importModal').classList.remove('hidden');
            });
            
            document.getElementById('uploadState').addEventListener('click', () => {
                document.getElementById('stateFileInput').click();
            });
            
            document.getElementById('stateFileInput').addEventListener('change', (e) => {
                const file = e.target.files[0];
                if (!file) return;
                
                const reader = new FileReader();
                reader.onload = (event) => {
                    document.getElementById('importInput').value = event.target.result;
                    document.getElementById('importModal').classList.remove('hidden');
                };
                reader.readAsText(file);
                e.target.value = '';
            });
            
            document.getElementById('downloadState').addEventListener('click', () => {
                const state = exportState();
                downloadJSON(state, `aggregator_state_${getTimestamp()}.json`);
            });
            
            // Download all responses
            document.getElementById('downloadAll').addEventListener('click', () => {
                const hasContent = AppState.models.responses.some(r => r.trim());
                if (!hasContent) {
                    showToast('No responses to download');
                    return;
                }
                
                let content = 'Advanced Model Aggregator Export\n';
                content += '================================\n\n';
                content += `Export Date: ${new Date().toLocaleString()}\n\n`;
                
                if (AppState.conversation.history.length > 0) {
                    content += 'Conversation History:\n';
                    content += AppState.conversation.history.join('\n\n') + '\n\n';
                }
                
                AppState.models.responses.forEach((response, index) => {
                    if (response.trim()) {
                        content += `=== Box ${index + 1} (${AppState.models.selected[index] || 'No model'}) ===\n`;
                        content += response + '\n\n';
                    }
                });
                
                downloadText(content, `all_responses_${getTimestamp()}.txt`);
            });
            
            // Clear all
            document.getElementById('clearAll').addEventListener('click', clearAllResponses);
            
            // Randomize all
            document.getElementById('randomizeAll').addEventListener('click', randomizeAllModels);
            
            // Preview buttons
            document.querySelectorAll('[data-preview]').forEach(btn => {
                btn.addEventListener('click', () => {
                    const index = parseInt(btn.dataset.preview);
                    previewResponse(index);
                });
            });
            
            document.getElementById('copyPreview').addEventListener('click', () => {
                const content = document.getElementById('previewContent').textContent;
                if (content && content !== 'Select a response to preview...') {
                    copyToClipboard(content);
                } else {
                    showToast('No content to copy');
                }
            });
            
            // Collapsible sections
            document.getElementById('previewHeader').addEventListener('click', (e) => {
                if (e.target.closest('button')) return;
                const section = document.getElementById('previewSection');
                section.classList.toggle('collapsed');
            });
            
            document.getElementById('logHeader').addEventListener('click', (e) => {
                if (e.target.closest('button')) return;
                const section = document.getElementById('logSection');
                section.classList.toggle('collapsed');
            });
            
            // Log actions
            document.getElementById('clearLog').addEventListener('click', clearLog);
            
            document.getElementById('copyLog').addEventListener('click', () => {
                if (AppState.log.length === 0) {
                    showToast('No log to copy');
                    return;
                }
                
                const text = AppState.log.map(e => 
                    `[${e.type}] ${e.timestamp} - ${e.title}\n${e.content}\n`
                ).join('\n');
                copyToClipboard(text);
            });
            
            // Mobile settings toggle
            const settingsToggle = document.getElementById('settingsToggle');
            if (settingsToggle) {
                settingsToggle.addEventListener('click', () => {
                    const controls = document.getElementById('topControls');
                    controls.classList.toggle('hidden');
                });
            }
            
            // PWA detection
            if ('serviceWorker' in navigator) {
                // Could register a service worker here for offline functionality
            }
            
            // Performance monitoring
            if ('PerformanceObserver' in window) {
                const perfObserver = new PerformanceObserver((list) => {
                    for (const entry of list.getEntries()) {
                        if (entry.duration > 100) {
                            console.warn('Slow interaction:', entry.name, entry.duration);
                        }
                    }
                });
                perfObserver.observe({ entryTypes: ['measure'] });
            }
            
            showToast('Model Aggregator Ready! 🚀');
        });
        
        // Global functions for onclick handlers
        window.toggleAccordion = toggleAccordion;
        window.selectModel = selectModel;
        window.selectRandomModel = selectRandomModel;
        window.previewResponse = previewResponse;
        window.copyResponse = copyResponse;
        window.downloadResponse = downloadResponse;
        window.clearResponse = clearResponse;
        window.clearModel = clearModel;
        window.toggleSection = toggleSection;
        window.closeImportModal = closeImportModal;
        window.importState = importState;
        window.closeAdvancedModal = closeAdvancedModal;
        window.applyAdvancedSettings = applyAdvancedSettings;
        
        // Prevent memory leaks on page unload
        window.addEventListener('beforeunload', () => {
            saveStateToStorage();
        });
    </script>
</body>
</html>

